# SmartUSBHub Pro 4CH USB3.0 历史文档

本目录保存 USB3.0 四口版本仍需保留的历史资料：

- `source/可编程USB3.0 5Gbps集线器数据手册.docx`：可编辑数据手册源文件；
- `API使用文档.md`：API 文档 Markdown；
- 同名 PDF：历史发布副本，默认由 `.gitignore` 排除。

后续如重新制作正式说明书，应把可编辑 DOCX 放入 `source/`，不要再创建顶层
`docs/product-sources/`。
